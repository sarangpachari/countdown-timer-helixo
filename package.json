{
  "name": "countdown-timer-app",
  "version": "1.0.0",
  "description": "Shopify Countdown Timer App — MERN Stack",
  "scripts": {
    "dev": "shopify app dev",
    "build": "cd web && npm run build",
    "deploy": "shopify app deploy",
    "info": "shopify app info"
  },
  "workspaces": [
    "web"
  ],
  "engines": {
    "node": ">=18.20.0"
  }
}
